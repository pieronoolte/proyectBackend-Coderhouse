<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ecomerce || Home</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</head>

<body>
  <div>
    <nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
      <div class="container-fluid">
        <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarNav">
          <a class="navbar-brand text-primary-emphasis fs-1">E-comerce</a>
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/products/myproducts'>My Products</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/carts/{{user.cart}}'>Cart</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/login'>Log in</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/register'>Register</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href="/api/sessions/logout">Logout</a>
            </li>
            <li class="nav-item">
              <button class="btn btn-outline-primary mx-3 mb-2">{{user.name}} {{user.lastname}}</button>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <h2 class="text-primary-emphasis my-3">Products:</h2>
    <div id="productsList" class="d-flex flex-wrap m-2" data-cart-id="{{user.cart}}">
      {{#if user}}
      {{#each products}}
      <div class="card p-3 m-5" style="width: 20rem;">
        <img src="/images/imageProduct.png" class="card-img-top" alt="imageProduct">
        <div class="card-body">
          <h3 class="card-title">{{this.title}}</h3>
          <h5 class="card-title">$ {{this.price}}</h5>
          <h5 class="card-text">{{this.description}}</h5>
          <button class="addProduct btn btn-primary" data-id="{{this._id}}">Agregar</button>
        </div>
      </div>
      {{/each}}
      {{else}}
      {{#each products}}
      <div class="card p-3 m-5" style="width: 20rem;">
        <img src="/images/imageProduct.png" class="card-img-top" alt="imageProduct">
        <div class="card-body">
          <h3 class="card-title">{{this.title}}</h3>
          <h5 class="card-title">$ {{this.price}}</h5>
          <h5 class="card-text">{{this.description}}</h5>
        </div>
      </div>
      {{/each}}
      {{/if}}
    </div>
  </div>

  <script src="/js/home.js"></script>
</body>

</html>
