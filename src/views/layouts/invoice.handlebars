<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modificación de rol de Usuario</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</head>

<body>
  <nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
      <div class="container-fluid">
        <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarNav">
          <a class="navbar-brand text-primary-emphasis fs-1" href='/api/home'>E-comerce</a>
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4">My Products</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/carts/{{user.cart}}'>Cart</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/login'>Log in</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/register'>Register</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-primary-emphasis fs-4" href="/api/sessions/logout">Logout</a>
            </li>
            <li class="nav-item">
              <button class="btn btn-outline-primary mx-3 mb-2">{{user.name}} {{user.lastname}}</button>
            </li>
          </ul>
        </div>
      </div>
    </nav>

  <h1 class="ms-5 text-primary-emphasis">Gracias por su compra, {{user.name}} {{user.lastname}}</h1>

  {{#each this}}
  {{#if _id}}
  <div id="cart">
    <ul id="productsList" class="row g-3 border mt-2 m-5 border-2 rounded border-primary-subtle list-unstyled fs-2">
      {{#each products}}
      <li>
        <div class="row mb-3 mt-5">
          <div class="col d-flex flex-row"><p class="m-2 text-primary-emphasis">Name:</p> <p class="m-2">{{this.product.title}}</p></div>
          <div class="col d-flex flex-row"><p class="m-2 text-primary-emphasis">Price:</p> <p class="productPrice m-2">{{this.product.price}}</p></div>
          <div class="col d-flex flex-row"><p class="m-2 text-primary-emphasis">Quantity:</p> <p class="productQuantity m-2">{{this.quantity}}</p></div>
        </div>
      </li>

      {{/each}}
    </ul>
    <div class="row mb-3 mt-5 border rounded border-2 border-primary-subtle m-5">
      <div class="col-5 d-flex flex-row"><p class="m-2 text-primary-emphasis fs-3">Total de Productos:</p> <p id="cart__quantity" class="m-2 fs-3"></p></div>
      <div class="col-4 d-flex flex-row"><p class="m-2 text-primary-emphasis fs-3">Precio Total:</p> <p id="cart__price" class="m-2 fs-3"></p></div>
      <button id="finishPay" class="payCart col-2 btn btn-primary m-2 fs-4" data-id="{{this._id}}">Finalizar compra</button>
    </div>

  </div>
  {{/if}}
  {{/each}}

  <script src="/js/invoice.js"></script>
</body>

</html>
