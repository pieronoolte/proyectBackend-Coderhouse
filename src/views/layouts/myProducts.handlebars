<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ecomerce || My Products</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</head>

<body>
  <nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
    <div class="container-fluid">
      <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarNav">
        <div><a class="navbar-brand text-primary-emphasis fs-1">E-comerce</a></div>
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link text-primary-emphasis fs-4" href='/api/home'>Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-primary-emphasis fs-4" href='/api/carts/{{user.cart}}'>Cart</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/login'>Log in</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-primary-emphasis fs-4" href='/api/sessions/register'>Register</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-primary-emphasis fs-4" href="/api/sessions/logout">Logout</a>
          </li>
          <li class="nav-item">
            <button class="btn btn-outline-primary mx-3 mb-2">{{user.name}} {{user.lastname}}</button>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <div>
    <h1 class="ms-5 text-primary-emphasis"> My Products:</h1>
    <ul id="productsList" class="list-unstyled">
    {{#if products.length}}
      {{#each products}}
      <li>
        {{!-- <div>
          Name: <p>{{this.title}}</p> </br>
          Price: <p>{{this.price}}</p>
          <button class="deleteProduct" data-id="{{this._id}}">delete</button>
        </div> --}}
        <div class="card p-3 m-5" style="width: 20rem;">
          <img src="/images/imageProduct.png" class="card-img-top" alt="imageProduct">
          <div class="card-body">
            <h3 class="card-title">{{this.title}}</h3>
            <h5 class="card-title">$ {{this.price}}</h5>
            <h5 class="card-text">{{this.description}}</h5>
            <div>
              <a class="btn btn-primary text-decoration-none text-white" href="/api/products/{{this._id}}">Update</a>
              <button class=" deleteProduct btn btn-primary" data-id="{{this._id}}">Delete</button>
            </div>
          </div>
        </div>
      </li>
      {{/each}}
      {{else}}
      <li>No tienes productos a la venta.</li>
      {{/if}}
    </ul>
    <button class="btn btn-primary"> <a class="text-white text-decoration-none" href="/api/products/new">Agregar Producto nuevo</a></button>
  </div>

  <script src="/js/myProducts.js"></script>
</body>

</html>
