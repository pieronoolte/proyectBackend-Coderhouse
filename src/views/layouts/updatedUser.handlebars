<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ecomerce || Modificación de rol de Usuario</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</head>

<body>
  <div class="d-flex justify-content-center align-items-center mt-5">
    <h1 class="mb-2 text-primary">Modificación de rol de Usuario</h1>
  </div>

  <div class="d-flex justify-content-center align-items-center mt-3">

    <form id="roleForm" class="row g-3 w-50 border mt-2 m-5 border-2 rounded border-primary-subtle"
      data-id="{{user._id}}">
      <ul>
        <li class="row m-2   text-primary-emphasis">Name: {{user.name}}</li>
        <li class="row m-2  text-primary-emphasis">Last name: {{user.lastname}}</li>
        <li class="row m-2  text-primary-emphasis">Birthdate: {{user.birthdate}}</li>
        <li class="row m-2  text-primary-emphasis">Email: {{user.email}}</li>
        <li class="row m-2  text-primary-emphasis">Role: {{user.role}}</li>
        <li class="row text-primary-emphasis">
          <div class="col-6 m-2">Select NewRole:</div>
          <select name="role" id="roleSelect"
            class="col-4 text-primary-emphasis border-2 rounded border-primary-subtle">
            <option value="client">Client</option>
            <option value="admin">Admin</option>
            <option value="premium">Premium</option>
          </select>
        </li>
      </ul>
      <div class="col-12 text-center mb-3">
        <button type="submit" class="btn btn-primary text-center">Update</button>
      </div>
    </form>
  </div>

  <div class="d-flex justify-content-center align-items-center mt-5 row mb-3 mt-5">
    <h2 class="mb-2 text-primary">Usuarios Inactivos:</h2>
    <div class="col-sm-10">
      <button id="inactiveUsers" class="col-2 btn btn-primary m-2 fs-4">Eliminar</button>
    </div>
  </div>

  <script src="/js/updatedUser.js"></script>
</body>

</html>
